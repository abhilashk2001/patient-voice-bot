# Secrets — never commit
.env
.env.local
*.key
*.pem

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# OS
.DS_Store

# Challenge brief PDFs (provider's materials — not redistributed)
*.pdf

# Local Claude Code tooling (not part of the deliverable)
claude/

# NOTE: calls/ recordings + transcripts ARE committed (required deliverables).
# Do not ignore calls/**/recording.* or transcript.txt.
