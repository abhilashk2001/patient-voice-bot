# Core runtime
python-dotenv>=1.0
openai>=1.40           # OpenAI Realtime API client
twilio>=9.0            # Telephony (outbound call + Media Streams)
websockets>=12.0       # Twilio <-> Realtime audio bridge

# Dev / test
pytest>=8.0

# System dependency (not pip-installable): ffmpeg  — used to convert/mux
# recordings to mp3. Install via `brew install ffmpeg`.
